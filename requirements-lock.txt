# Exact versions the frozen recipe (2026-09-freeze) and Study 2 were run with
# (Python 3.12.11, Linux x86_64). Install with:
#   pip install -r requirements-lock.txt
# torch was installed from the CUDA 13.0 wheel index (2.12.0+cu130); the CPU
# wheel of the same version produces identical transcripts more slowly.
openai-whisper==20250625
torch==2.12.0
pandas==2.3.3
numpy==2.4.2
scipy==1.17.1
openpyxl==3.1.5
matplotlib==3.11.0
scikit-learn==1.8.0
tiktoken==0.13.0
pytest==9.0.3
